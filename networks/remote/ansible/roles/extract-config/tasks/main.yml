---

- name: Fetch genesis.json
  fetch: "src=/home/nodef/.nodef/config/genesis.json dest={{GENESISFILE}} flat=yes"
  run_once: yes
  become: yes
  become_user: nodef

- name: Fetch config.toml
  fetch: "src=/home/nodef/.nodef/config/config.toml dest={{CONFIGFILE}} flat=yes"
  run_once: yes
  become: yes
  become_user: nodef

