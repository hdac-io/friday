---

- name: Stop service
  service: name=nodef state=stopped

- name: Delete files
  file: "path={{item}} state=absent"
  with_items:
  - /usr/bin/nodef
  - /home/nodef/.nodef
  - /home/nodef/.clif

